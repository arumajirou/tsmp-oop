# managed snippet (tracked in Git)
local   all         postgres                                peer
local   all         all                                     peer
host    appdb       appuser          127.0.0.1/32           scram-sha-256
